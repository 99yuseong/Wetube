mixin video(videos)
    each video in videos
        video(src=`/${videos.videoUrl}`)
        Strong #{video.title}
        small #{video.channel.name}
        small #{video.meta.views} #{video.meta.views > 1 ? "views" : "view" } 
        small #{video.meta.createdAt}
